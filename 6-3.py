vacabulary={'if':"如果",'del':'删除','append':'在列表末尾添加元素',
            'sort':'整理列表元素','insert':'在某处插入元素',
            'set':'创造一个元素独一无二的集合',
            'sorted':'对列表进行排序','items()':'获取字典中所有的项',
            'keys()':'获取字典中所有的键值','values()':'获取字典中所有的值',}

for key,value in vacabulary.items():
    print(key+':')
    print('\t'+str(value))

